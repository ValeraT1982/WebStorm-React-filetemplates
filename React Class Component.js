import * as React from 'react';
import PropTypes from 'prop-types';

class ${NAME} extends React.Component {
  render() {
    return (
      <div>
      </div>
    );
  }
}

${NAME}.propTypes = {
};

${NAME}.defaultProps = {
};

export default ${NAME}